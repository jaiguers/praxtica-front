/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'api.dicebear.com', 'avatars.githubusercontent.com'],
  },
};

module.exports = nextConfig; 